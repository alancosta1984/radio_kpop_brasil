name: radio_kpop_brasil
description: Rádio K-POP Brasil
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png

name: radio_kpop_brasil
description: Rádio K-POP Brasil
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.10.3
  audio_session: ^0.2.2

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
